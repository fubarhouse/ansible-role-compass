---

- hosts: localhost
  sudo: true
  vars:
    ansible_ssh_user: travis
    ruby_version: 2.3.0
    ruby_versions: []
    ruby_packages:
      - sass
  roles:
    - { role: ../../ansible-role-ruby }

