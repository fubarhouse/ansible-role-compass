---

- hosts: all
  sudo: true
  vars:
    ansible_ssh_user: root
    ruby_version: 2.3.0
    ruby_versions:
      - 2.3.0
    ruby_packages:
      - sass
  roles:
    - { role: ../../fubarhouse.ruby }
