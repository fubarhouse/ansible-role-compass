---

- hosts: localhost
  sudo: true
  vars:
#    ansible_ssh_user: travis
    ruby_version: 2.3.1
    ruby_versions:
      - 2.2.5
    ruby_packages:
      - sass
  roles:
    - { role: ../../ansible-role-ruby }

